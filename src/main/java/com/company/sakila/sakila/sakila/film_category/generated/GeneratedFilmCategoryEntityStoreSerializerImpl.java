package com.company.sakila.sakila.sakila.film_category.generated;

import com.company.sakila.sakila.sakila.category.Category;
import com.company.sakila.sakila.sakila.film.Film;
import com.company.sakila.sakila.sakila.film_category.FilmCategory;
import com.company.sakila.sakila.sakila.film_category.FilmCategoryImpl;
import com.speedment.common.annotation.GeneratedCode;
import com.speedment.common.function.BiLongToIntFunction;
import com.speedment.common.function.LongToBooleanFunction;
import com.speedment.common.function.LongToByteFunction;
import com.speedment.common.function.LongToCharFunction;
import com.speedment.common.function.LongToFloatFunction;
import com.speedment.common.function.LongToShortFunction;
import com.speedment.enterprise.datastore.runtime.DataStoreHolder;
import com.speedment.enterprise.datastore.runtime.entitystore.EntityStoreSerializer;
import com.speedment.enterprise.datastore.runtime.entitystore.function.EntityStoreComparator;
import com.speedment.enterprise.datastore.runtime.entitystore.function.EntityStoreCompareTo;
import com.speedment.enterprise.datastore.runtime.entitystore.function.EntityStorePredicate;
import com.speedment.enterprise.datastore.runtime.fieldcache.FieldCache;
import com.speedment.enterprise.datastore.runtime.util.SerializerUtil;
import com.speedment.runtime.config.identifier.ColumnIdentifier;

import java.nio.ByteBuffer;
import java.sql.Timestamp;
import java.util.List;
import java.util.function.BiConsumer;
import java.util.function.LongConsumer;
import java.util.function.LongFunction;
import java.util.function.LongPredicate;
import java.util.function.LongToDoubleFunction;
import java.util.function.LongToIntFunction;
import java.util.function.LongUnaryOperator;

import static java.util.Collections.singletonList;
import static java.util.Objects.requireNonNull;
import static java.util.stream.Collectors.joining;

/**
 * Serializes and deserializes instances of FilmCategory.
 * <p>
 * This file has been automatically generated by Speedment. Any changes made to
 * it will be overwritten.
 * 
 * @author Speedment
 */
@GeneratedCode("Speedment")
public abstract class GeneratedFilmCategoryEntityStoreSerializerImpl implements EntityStoreSerializer<FilmCategory> {
    
    private final LongFunction<ByteBuffer> bufferFinder;
    private final LongToIntFunction offsetFinder;
    private final static int FIELD_CATEGORY_ID = 0;
    private final static int FIELD_FILM_ID = 2;
    private final static int FIELD_LAST_UPDATE = 6;
    private final static int FKREF_FK_FILM_CATEGORY_CATEGORY = 14;
    private final static int FKREF_FK_FILM_CATEGORY_FILM = 22;
    private final static int VARSIZE_BEGINS = 30;
    
    protected GeneratedFilmCategoryEntityStoreSerializerImpl(final LongFunction<ByteBuffer> bufferFinder, final LongToIntFunction offsetFinder) {
        this.bufferFinder = requireNonNull(bufferFinder);
        this.offsetFinder = requireNonNull(offsetFinder);
    }
    
    @Override
    public BiConsumer<ByteBuffer, FilmCategory> serializer() {
        return (buffer, entity) -> {
            buffer.putShort(FIELD_CATEGORY_ID, entity.getCategoryId());
            buffer.putInt(FIELD_FILM_ID, entity.getFilmId());
            buffer.putLong(FIELD_LAST_UPDATE, entity.getLastUpdate().getTime());
            buffer.putLong(FKREF_FK_FILM_CATEGORY_CATEGORY, -1L); // Will be set later on in the decorator()-method.
            buffer.putLong(FKREF_FK_FILM_CATEGORY_FILM, -1L); // Will be set later on in the decorator()-method.
            buffer.position(0);
            buffer.limit(VARSIZE_BEGINS);
        };
    }
    
    @Override
    public LongConsumer decorator(DataStoreHolder holder) {
        final FieldCache.OfShort category_categoryIdFieldCache = holder.getFieldCache(Category.CATEGORY_ID.identifier());
        final FieldCache.OfInt film_filmIdFieldCache = holder.getFieldCache(Film.FILM_ID.identifier());
        final LongToShortFunction categoryIdDeserializer = shortDeserializer(FilmCategory.CATEGORY_ID.identifier());
        final LongToIntFunction filmIdDeserializer = intDeserializer(FilmCategory.FILM_ID.identifier());
        final LongUnaryOperator fkFilmCategoryCategoryResolver = ref -> {
            final short value = categoryIdDeserializer.applyAsShort(ref);
            return category_categoryIdFieldCache.any(value);
        };
        final LongUnaryOperator fkFilmCategoryFilmResolver = ref -> {
            final int value = filmIdDeserializer.applyAsInt(ref);
            return film_filmIdFieldCache.any(value);
        };
        return ref -> {
            final ByteBuffer buffer = bufferFinder.apply(ref);
            final int rowOffset = offsetFinder.applyAsInt(ref);
            buffer.putLong(rowOffset + FKREF_FK_FILM_CATEGORY_CATEGORY, fkFilmCategoryCategoryResolver.applyAsLong(ref));
            buffer.putLong(rowOffset + FKREF_FK_FILM_CATEGORY_FILM, fkFilmCategoryFilmResolver.applyAsLong(ref));
        };
    }
    
    @Override
    public LongFunction<FilmCategory> deserializer() {
        return ref -> {
            final ByteBuffer buffer = bufferFinder.apply(ref);
            final int offset = offsetFinder.applyAsInt(ref);
            final FilmCategory entity = new FilmCategoryImpl();
            entity.setFilmId(buffer.getInt(offset + FIELD_FILM_ID));
            entity.setCategoryId(buffer.getShort(offset + FIELD_CATEGORY_ID));
            entity.setLastUpdate(new Timestamp(buffer.getLong(offset + FIELD_LAST_UPDATE)));
            return entity;
        };
    }
    
    @Override
    public Class<?> deserializedType(final ColumnIdentifier<FilmCategory> colId) {
        if (colId instanceof FilmCategory.Identifier) {
            final FilmCategory.Identifier _id = (FilmCategory.Identifier) colId;
            switch (_id) {
                case CATEGORY_ID : return short.class;
                case FILM_ID     : return int.class;
                case LAST_UPDATE : return Timestamp.class;
                default : {
                    throw new UnsupportedOperationException(
                        String.format("Unknown enum constant '%s'.", _id)
                    );
                }
            }
        } else {
            final String _colName = colId.getColumnId();
            switch (_colName) {
                case "category_id" : return short.class;
                case "film_id"     : return int.class;
                case "last_update" : return Timestamp.class;
                default : {
                    throw new UnsupportedOperationException(
                        String.format("Unknown column name '%s'.", _colName)
                    );
                }
            }
        }
    }
    
    @Override
    public <FK_ENTITY> LongUnaryOperator finder(final ColumnIdentifier<FilmCategory> colId, final ColumnIdentifier<FK_ENTITY> fkColId) {
        return finder(singletonList(colId), singletonList(fkColId));
    }
    
    @Override
    public <FK_ENTITY> LongUnaryOperator finder(final List<ColumnIdentifier<FilmCategory>> cols, final List<ColumnIdentifier<FK_ENTITY>> fkCols) {
        final String fkName = SerializerUtil.uniqueFkName(cols, fkCols);
        switch (fkName) {
            case "{category_id}->category{category_id}": return finder("fk_film_category_category");
            case "{film_id}->film{film_id}": return finder("fk_film_category_film");
        }
        
        throw new IllegalArgumentException(
            fkName + " is not a valid foreign reference name."
        );
    }
    
    @Override
    public LongUnaryOperator finder(final String fkName) {
        switch (fkName) {
            case "fk_film_category_category": return ref -> bufferFinder.apply(ref).getLong(offsetFinder.applyAsInt(ref) + FKREF_FK_FILM_CATEGORY_CATEGORY);
            case "fk_film_category_film": return ref -> bufferFinder.apply(ref).getLong(offsetFinder.applyAsInt(ref) + FKREF_FK_FILM_CATEGORY_FILM);
        }
        
        throw new IllegalArgumentException(
            "Could not find a foreign key " + fkName + " in table 'film_category'."
        );
    }
    
    @Override
    public LongPredicate isNull(final ColumnIdentifier<FilmCategory> colId) {
        if (colId instanceof FilmCategory.Identifier) {
            final FilmCategory.Identifier _id = (FilmCategory.Identifier) colId;
            switch (_id) {
                case CATEGORY_ID : 
                case FILM_ID     : 
                case LAST_UPDATE : return ALWAYS_FALSE;
                default : {
                    throw new UnsupportedOperationException(
                        String.format("Unknown enum constant '%s'.", _id)
                    );
                }
            }
        } else {
            final String _colName = colId.getColumnId();
            switch (_colName) {
                case "category_id" : 
                case "film_id"     : 
                case "last_update" : return ALWAYS_FALSE;
                default : {
                    throw new UnsupportedOperationException(
                        String.format("Unknown column name '%s'.", _colName)
                    );
                }
            }
        }
    }
    
    @Override
    public LongPredicate isPresent(final ColumnIdentifier<FilmCategory> colId) {
        if (colId instanceof FilmCategory.Identifier) {
            final FilmCategory.Identifier _id = (FilmCategory.Identifier) colId;
            switch (_id) {
                case CATEGORY_ID : 
                case FILM_ID     : 
                case LAST_UPDATE : return ALWAYS_TRUE;
                default : {
                    throw new UnsupportedOperationException(
                        String.format("Unknown enum constant '%s'.", _id)
                    );
                }
            }
        } else {
            final String _colName = colId.getColumnId();
            switch (_colName) {
                case "category_id" : 
                case "film_id"     : 
                case "last_update" : return ALWAYS_TRUE;
                default : {
                    throw new UnsupportedOperationException(
                        String.format("Unknown column name '%s'.", _colName)
                    );
                }
            }
        }
    }
    
    @Override
    public LongToByteFunction byteDeserializer(final ColumnIdentifier<FilmCategory> colId) {
        throw new UnsupportedOperationException(
            String.format("The column '%s' is either unknown or not of type byte.", colId.getColumnId())
        );
    }
    
    @Override
    public LongToShortFunction shortDeserializer(final ColumnIdentifier<FilmCategory> colId) {
        if ("category_id".equals(colId.getColumnId())) {
            return ref -> bufferFinder.apply(ref).getShort(offsetFinder.applyAsInt(ref) + FIELD_CATEGORY_ID);
        }
        
        throw new UnsupportedOperationException(
            String.format("The column '%s' is either unknown or not of type short.", colId.getColumnId())
        );
    }
    
    @Override
    public LongToIntFunction intDeserializer(final ColumnIdentifier<FilmCategory> colId) {
        if ("film_id".equals(colId.getColumnId())) {
            return ref -> bufferFinder.apply(ref).getInt(offsetFinder.applyAsInt(ref) + FIELD_FILM_ID);
        }
        
        throw new UnsupportedOperationException(
            String.format("The column '%s' is either unknown or not of type int.", colId.getColumnId())
        );
    }
    
    @Override
    public LongUnaryOperator longDeserializer(final ColumnIdentifier<FilmCategory> colId) {
        throw new UnsupportedOperationException(
            String.format("The column '%s' is either unknown or not of type long.", colId.getColumnId())
        );
    }
    
    @Override
    public LongToFloatFunction floatDeserializer(final ColumnIdentifier<FilmCategory> colId) {
        throw new UnsupportedOperationException(
            String.format("The column '%s' is either unknown or not of type float.", colId.getColumnId())
        );
    }
    
    @Override
    public LongToDoubleFunction doubleDeserializer(final ColumnIdentifier<FilmCategory> colId) {
        throw new UnsupportedOperationException(
            String.format("The column '%s' is either unknown or not of type double.", colId.getColumnId())
        );
    }
    
    @Override
    public LongToCharFunction charDeserializer(final ColumnIdentifier<FilmCategory> colId) {
        throw new UnsupportedOperationException(
            String.format("The column '%s' is either unknown or not of type char.", colId.getColumnId())
        );
    }
    
    @Override
    public LongToBooleanFunction booleanDeserializer(final ColumnIdentifier<FilmCategory> colId) {
        throw new UnsupportedOperationException(
            String.format("The column '%s' is either unknown or not of type boolean.", colId.getColumnId())
        );
    }
    
    @Override
    public LongFunction<?> objectDeserializer(final ColumnIdentifier<FilmCategory> colId) {
        if ("last_update".equals(colId.getColumnId())) {
            return ref -> new Timestamp(bufferFinder.apply(ref).getLong(offsetFinder.applyAsInt(ref) + FIELD_LAST_UPDATE));
        }
        
        throw new UnsupportedOperationException(
            String.format("Unknown column name '%s'.", colId.getColumnId())
        );
    }
    
    @Override
    public BiLongToIntFunction comparator(final ColumnIdentifier<FilmCategory> colId) {
        if (colId instanceof FilmCategory.Identifier) {
            final FilmCategory.Identifier _id = (FilmCategory.Identifier) colId;
            switch (_id) {
                case CATEGORY_ID : return (aRef, bRef) -> Short.compare(
                    bufferFinder.apply(aRef).getShort(offsetFinder.applyAsInt(aRef) + FIELD_CATEGORY_ID),
                    bufferFinder.apply(bRef).getShort(offsetFinder.applyAsInt(bRef) + FIELD_CATEGORY_ID)
                );
                case FILM_ID     : return (aRef, bRef) -> Integer.compare(
                    bufferFinder.apply(aRef).getInt(offsetFinder.applyAsInt(aRef) + FIELD_FILM_ID),
                    bufferFinder.apply(bRef).getInt(offsetFinder.applyAsInt(bRef) + FIELD_FILM_ID)
                );
                case LAST_UPDATE : return (aRef, bRef) -> Long.compare(
                    bufferFinder.apply(aRef).getLong(offsetFinder.applyAsInt(aRef) + FIELD_LAST_UPDATE),
                    bufferFinder.apply(bRef).getLong(offsetFinder.applyAsInt(bRef) + FIELD_LAST_UPDATE)
                );
                default : {
                    throw new UnsupportedOperationException(
                        String.format("Unknown enum constant '%s'.", _id)
                    );
                }
            }
        } else {
            final String _colName = colId.getColumnId();
            switch (_colName) {
                case "category_id" : return (aRef, bRef) -> Short.compare(
                    bufferFinder.apply(aRef).getShort(offsetFinder.applyAsInt(aRef) + FIELD_CATEGORY_ID),
                    bufferFinder.apply(bRef).getShort(offsetFinder.applyAsInt(bRef) + FIELD_CATEGORY_ID)
                );
                case "film_id"     : return (aRef, bRef) -> Integer.compare(
                    bufferFinder.apply(aRef).getInt(offsetFinder.applyAsInt(aRef) + FIELD_FILM_ID),
                    bufferFinder.apply(bRef).getInt(offsetFinder.applyAsInt(bRef) + FIELD_FILM_ID)
                );
                case "last_update" : return (aRef, bRef) -> Long.compare(
                    bufferFinder.apply(aRef).getLong(offsetFinder.applyAsInt(aRef) + FIELD_LAST_UPDATE),
                    bufferFinder.apply(bRef).getLong(offsetFinder.applyAsInt(bRef) + FIELD_LAST_UPDATE)
                );
                default : {
                    throw new UnsupportedOperationException(
                        String.format("Unknown column name '%s'.", _colName)
                    );
                }
            }
        }
    }
    
    @Override
    public BiLongToIntFunction comparatorNullsLast(final ColumnIdentifier<FilmCategory> colId) {
        if (colId instanceof FilmCategory.Identifier) {
            final FilmCategory.Identifier _id = (FilmCategory.Identifier) colId;
            switch (_id) {
                case CATEGORY_ID : return (aRef, bRef) -> Short.compare(
                    bufferFinder.apply(aRef).getShort(offsetFinder.applyAsInt(aRef) + FIELD_CATEGORY_ID),
                    bufferFinder.apply(bRef).getShort(offsetFinder.applyAsInt(bRef) + FIELD_CATEGORY_ID)
                );
                case FILM_ID     : return (aRef, bRef) -> Integer.compare(
                    bufferFinder.apply(aRef).getInt(offsetFinder.applyAsInt(aRef) + FIELD_FILM_ID),
                    bufferFinder.apply(bRef).getInt(offsetFinder.applyAsInt(bRef) + FIELD_FILM_ID)
                );
                case LAST_UPDATE : return (aRef, bRef) -> Long.compare(
                    bufferFinder.apply(aRef).getLong(offsetFinder.applyAsInt(aRef) + FIELD_LAST_UPDATE),
                    bufferFinder.apply(bRef).getLong(offsetFinder.applyAsInt(bRef) + FIELD_LAST_UPDATE)
                );
                default : {
                    throw new UnsupportedOperationException(
                        String.format("Unknown enum constant '%s'.", _id)
                    );
                }
            }
        } else {
            final String _colName = colId.getColumnId();
            switch (_colName) {
                case "category_id" : return (aRef, bRef) -> Short.compare(
                    bufferFinder.apply(aRef).getShort(offsetFinder.applyAsInt(aRef) + FIELD_CATEGORY_ID),
                    bufferFinder.apply(bRef).getShort(offsetFinder.applyAsInt(bRef) + FIELD_CATEGORY_ID)
                );
                case "film_id"     : return (aRef, bRef) -> Integer.compare(
                    bufferFinder.apply(aRef).getInt(offsetFinder.applyAsInt(aRef) + FIELD_FILM_ID),
                    bufferFinder.apply(bRef).getInt(offsetFinder.applyAsInt(bRef) + FIELD_FILM_ID)
                );
                case "last_update" : return (aRef, bRef) -> Long.compare(
                    bufferFinder.apply(aRef).getLong(offsetFinder.applyAsInt(aRef) + FIELD_LAST_UPDATE),
                    bufferFinder.apply(bRef).getLong(offsetFinder.applyAsInt(bRef) + FIELD_LAST_UPDATE)
                );
                default : {
                    throw new UnsupportedOperationException(
                        String.format("Unknown column name '%s'.", _colName)
                    );
                }
            }
        }
    }
    
    @Override
    public LongToIntFunction compareToByte(final ColumnIdentifier<FilmCategory> colId, final byte value) {
        throw new UnsupportedOperationException(
            String.format("The column '%s' is either unknown or not of type byte.", colId.getColumnId())
        );
    }
    
    @Override
    public LongToIntFunction compareToShort(final ColumnIdentifier<FilmCategory> colId, final short value) {
        if ("category_id".equals(colId.getColumnId())) {
            {
                final short operand = value;
                return ref -> Short.compare(
                    bufferFinder.apply(ref).getShort(offsetFinder.applyAsInt(ref) + FIELD_CATEGORY_ID),
                    operand
                );
            }
        }
        
        throw new UnsupportedOperationException(
            String.format("The column '%s' is either unknown or not of type short.", colId.getColumnId())
        );
    }
    
    @Override
    public LongToIntFunction compareToInt(final ColumnIdentifier<FilmCategory> colId, final int value) {
        if ("film_id".equals(colId.getColumnId())) {
            {
                final int operand = value;
                return ref -> Integer.compare(
                    bufferFinder.apply(ref).getInt(offsetFinder.applyAsInt(ref) + FIELD_FILM_ID),
                    operand
                );
            }
        }
        
        throw new UnsupportedOperationException(
            String.format("The column '%s' is either unknown or not of type int.", colId.getColumnId())
        );
    }
    
    @Override
    public LongToIntFunction compareToLong(final ColumnIdentifier<FilmCategory> colId, final long value) {
        throw new UnsupportedOperationException(
            String.format("The column '%s' is either unknown or not of type long.", colId.getColumnId())
        );
    }
    
    @Override
    public LongToIntFunction compareToFloat(final ColumnIdentifier<FilmCategory> colId, final float value) {
        throw new UnsupportedOperationException(
            String.format("The column '%s' is either unknown or not of type float.", colId.getColumnId())
        );
    }
    
    @Override
    public LongToIntFunction compareToDouble(final ColumnIdentifier<FilmCategory> colId, final double value) {
        throw new UnsupportedOperationException(
            String.format("The column '%s' is either unknown or not of type double.", colId.getColumnId())
        );
    }
    
    @Override
    public LongToIntFunction compareToBoolean(final ColumnIdentifier<FilmCategory> colId, final boolean value) {
        throw new UnsupportedOperationException(
            String.format("The column '%s' is either unknown or not of type boolean.", colId.getColumnId())
        );
    }
    
    @Override
    public LongToIntFunction compareToChar(final ColumnIdentifier<FilmCategory> colId, final char value) {
        throw new UnsupportedOperationException(
            String.format("The column '%s' is either unknown or not of type char.", colId.getColumnId())
        );
    }
    
    @Override
    public LongToIntFunction compareToObject(final ColumnIdentifier<FilmCategory> colId, final Object value) {
        if ("last_update".equals(colId.getColumnId())) {
            {
                final long operand = ((Timestamp) value).getTime();
                return ref -> Long.compare(
                    bufferFinder.apply(ref).getLong(offsetFinder.applyAsInt(ref) + FIELD_LAST_UPDATE),
                    operand
                );
            }
        }
        
        throw new UnsupportedOperationException(
            String.format("The column '%s' is either unknown or not of type object.", colId.getColumnId())
        );
    }
    
    @Override
    public LongToIntFunction compareToNull(final ColumnIdentifier<FilmCategory> colId) {
        if (colId instanceof FilmCategory.Identifier) {
            final FilmCategory.Identifier _id = (FilmCategory.Identifier) colId;
            switch (_id) {
                case CATEGORY_ID : 
                case FILM_ID     : 
                case LAST_UPDATE : return ALWAYS_LESS;
                default : {
                    throw new UnsupportedOperationException(
                        String.format("Unknown enum constant '%s'.", _id)
                    );
                }
            }
        } else {
            final String _colName = colId.getColumnId();
            switch (_colName) {
                case "category_id" : 
                case "film_id"     : 
                case "last_update" : return ALWAYS_LESS;
                default : {
                    throw new UnsupportedOperationException(
                        String.format("Unknown column name '%s'.", _colName)
                    );
                }
            }
        }
    }
    
    @Override
    public LongPredicate newPredicate(final ColumnIdentifier<FilmCategory> colId, final EntityStorePredicate predicate) {
        if (colId instanceof FilmCategory.Identifier) {
            final FilmCategory.Identifier _id = (FilmCategory.Identifier) colId;
            switch (_id) {
                case CATEGORY_ID : return ref -> {
                    final ByteBuffer buffer = bufferFinder.apply(ref);
                    final int rowOffset = offsetFinder.applyAsInt(ref);
                    final int begins = rowOffset + FIELD_CATEGORY_ID;
                    return predicate.test(buffer, begins, begins + Short.BYTES);
                };
                case FILM_ID     : return ref -> {
                    final ByteBuffer buffer = bufferFinder.apply(ref);
                    final int rowOffset = offsetFinder.applyAsInt(ref);
                    final int begins = rowOffset + FIELD_FILM_ID;
                    return predicate.test(buffer, begins, begins + Integer.BYTES);
                };
                case LAST_UPDATE : return ref -> {
                    final ByteBuffer buffer = bufferFinder.apply(ref);
                    final int rowOffset = offsetFinder.applyAsInt(ref);
                    final int begins = rowOffset + FIELD_LAST_UPDATE;
                    return predicate.test(buffer, begins, begins + 8);
                };
                default : {
                    throw new UnsupportedOperationException(
                        String.format("Unknown enum constant '%s'.", _id)
                    );
                }
            }
        } else {
            final String _colName = colId.getColumnId();
            switch (_colName) {
                case "category_id" : return ref -> {
                    final ByteBuffer buffer = bufferFinder.apply(ref);
                    final int rowOffset = offsetFinder.applyAsInt(ref);
                    final int begins = rowOffset + FIELD_CATEGORY_ID;
                    return predicate.test(buffer, begins, begins + Short.BYTES);
                };
                case "film_id"     : return ref -> {
                    final ByteBuffer buffer = bufferFinder.apply(ref);
                    final int rowOffset = offsetFinder.applyAsInt(ref);
                    final int begins = rowOffset + FIELD_FILM_ID;
                    return predicate.test(buffer, begins, begins + Integer.BYTES);
                };
                case "last_update" : return ref -> {
                    final ByteBuffer buffer = bufferFinder.apply(ref);
                    final int rowOffset = offsetFinder.applyAsInt(ref);
                    final int begins = rowOffset + FIELD_LAST_UPDATE;
                    return predicate.test(buffer, begins, begins + 8);
                };
                default : {
                    throw new UnsupportedOperationException(
                        String.format("Unknown column name '%s'.", _colName)
                    );
                }
            }
        }
    }
    
    @Override
    public LongToIntFunction newCompareTo(final ColumnIdentifier<FilmCategory> colId, final EntityStoreCompareTo compareTo) {
        if (colId instanceof FilmCategory.Identifier) {
            final FilmCategory.Identifier _id = (FilmCategory.Identifier) colId;
            switch (_id) {
                case CATEGORY_ID : return ref -> {
                    final ByteBuffer buffer = bufferFinder.apply(ref);
                    final int rowOffset = offsetFinder.applyAsInt(ref);
                    final int begins = rowOffset + FIELD_CATEGORY_ID;
                    return compareTo.compare(buffer, begins, begins + Short.BYTES);
                };
                case FILM_ID     : return ref -> {
                    final ByteBuffer buffer = bufferFinder.apply(ref);
                    final int rowOffset = offsetFinder.applyAsInt(ref);
                    final int begins = rowOffset + FIELD_FILM_ID;
                    return compareTo.compare(buffer, begins, begins + Integer.BYTES);
                };
                case LAST_UPDATE : return ref -> {
                    final ByteBuffer buffer = bufferFinder.apply(ref);
                    final int rowOffset = offsetFinder.applyAsInt(ref);
                    final int begins = rowOffset + FIELD_LAST_UPDATE;
                    return compareTo.compare(buffer, begins, begins + 8);
                };
                default : {
                    throw new UnsupportedOperationException(
                        String.format("Unknown enum constant '%s'.", _id)
                    );
                }
            }
        } else {
            final String _colName = colId.getColumnId();
            switch (_colName) {
                case "category_id" : return ref -> {
                    final ByteBuffer buffer = bufferFinder.apply(ref);
                    final int rowOffset = offsetFinder.applyAsInt(ref);
                    final int begins = rowOffset + FIELD_CATEGORY_ID;
                    return compareTo.compare(buffer, begins, begins + Short.BYTES);
                };
                case "film_id"     : return ref -> {
                    final ByteBuffer buffer = bufferFinder.apply(ref);
                    final int rowOffset = offsetFinder.applyAsInt(ref);
                    final int begins = rowOffset + FIELD_FILM_ID;
                    return compareTo.compare(buffer, begins, begins + Integer.BYTES);
                };
                case "last_update" : return ref -> {
                    final ByteBuffer buffer = bufferFinder.apply(ref);
                    final int rowOffset = offsetFinder.applyAsInt(ref);
                    final int begins = rowOffset + FIELD_LAST_UPDATE;
                    return compareTo.compare(buffer, begins, begins + 8);
                };
                default : {
                    throw new UnsupportedOperationException(
                        String.format("Unknown column name '%s'.", _colName)
                    );
                }
            }
        }
    }
    
    @Override
    public BiLongToIntFunction newComparator(final ColumnIdentifier<FilmCategory> colId, final EntityStoreComparator comparator) {
        if (colId instanceof FilmCategory.Identifier) {
            final FilmCategory.Identifier _id = (FilmCategory.Identifier) colId;
            switch (_id) {
                case CATEGORY_ID : return (aRef, bRef) -> {
                    final ByteBuffer aBuffer = bufferFinder.apply(aRef);
                    final ByteBuffer bBuffer = bufferFinder.apply(bRef);
                    final int aRowOffset = offsetFinder.applyAsInt(aRef);
                    final int bRowOffset = offsetFinder.applyAsInt(bRef);
                    final int aBegins = aRowOffset + FIELD_CATEGORY_ID;
                    final int bBegins = bRowOffset + FIELD_CATEGORY_ID;
                    return comparator.compare(
                        aBuffer, aBegins, aBegins + Short.BYTES,
                        bBuffer, bBegins, bBegins + Short.BYTES
                    );
                };
                case FILM_ID     : return (aRef, bRef) -> {
                    final ByteBuffer aBuffer = bufferFinder.apply(aRef);
                    final ByteBuffer bBuffer = bufferFinder.apply(bRef);
                    final int aRowOffset = offsetFinder.applyAsInt(aRef);
                    final int bRowOffset = offsetFinder.applyAsInt(bRef);
                    final int aBegins = aRowOffset + FIELD_FILM_ID;
                    final int bBegins = bRowOffset + FIELD_FILM_ID;
                    return comparator.compare(
                        aBuffer, aBegins, aBegins + Integer.BYTES,
                        bBuffer, bBegins, bBegins + Integer.BYTES
                    );
                };
                case LAST_UPDATE : return (aRef, bRef) -> {
                    final ByteBuffer aBuffer = bufferFinder.apply(aRef);
                    final ByteBuffer bBuffer = bufferFinder.apply(bRef);
                    final int aRowOffset = offsetFinder.applyAsInt(aRef);
                    final int bRowOffset = offsetFinder.applyAsInt(bRef);
                    final int aBegins = aRowOffset + FIELD_LAST_UPDATE;
                    final int bBegins = bRowOffset + FIELD_LAST_UPDATE;
                    return comparator.compare(
                        aBuffer, aBegins, aBegins + 8,
                        bBuffer, bBegins, bBegins + 8
                    );
                };
                default : {
                    throw new UnsupportedOperationException(
                        String.format("Unknown enum constant '%s'.", _id)
                    );
                }
            }
        } else {
            final String _colName = colId.getColumnId();
            switch (_colName) {
                case "category_id" : return (aRef, bRef) -> {
                    final ByteBuffer aBuffer = bufferFinder.apply(aRef);
                    final ByteBuffer bBuffer = bufferFinder.apply(bRef);
                    final int aRowOffset = offsetFinder.applyAsInt(aRef);
                    final int bRowOffset = offsetFinder.applyAsInt(bRef);
                    final int aBegins = aRowOffset + FIELD_CATEGORY_ID;
                    final int bBegins = bRowOffset + FIELD_CATEGORY_ID;
                    return comparator.compare(
                        aBuffer, aBegins, aBegins + Short.BYTES,
                        bBuffer, bBegins, bBegins + Short.BYTES
                    );
                };
                case "film_id"     : return (aRef, bRef) -> {
                    final ByteBuffer aBuffer = bufferFinder.apply(aRef);
                    final ByteBuffer bBuffer = bufferFinder.apply(bRef);
                    final int aRowOffset = offsetFinder.applyAsInt(aRef);
                    final int bRowOffset = offsetFinder.applyAsInt(bRef);
                    final int aBegins = aRowOffset + FIELD_FILM_ID;
                    final int bBegins = bRowOffset + FIELD_FILM_ID;
                    return comparator.compare(
                        aBuffer, aBegins, aBegins + Integer.BYTES,
                        bBuffer, bBegins, bBegins + Integer.BYTES
                    );
                };
                case "last_update" : return (aRef, bRef) -> {
                    final ByteBuffer aBuffer = bufferFinder.apply(aRef);
                    final ByteBuffer bBuffer = bufferFinder.apply(bRef);
                    final int aRowOffset = offsetFinder.applyAsInt(aRef);
                    final int bRowOffset = offsetFinder.applyAsInt(bRef);
                    final int aBegins = aRowOffset + FIELD_LAST_UPDATE;
                    final int bBegins = bRowOffset + FIELD_LAST_UPDATE;
                    return comparator.compare(
                        aBuffer, aBegins, aBegins + 8,
                        bBuffer, bBegins, bBegins + 8
                    );
                };
                default : {
                    throw new UnsupportedOperationException(
                        String.format("Unknown column name '%s'.", _colName)
                    );
                }
            }
        }
    }
    
    @Override
    public LongToIntFunction sizeOf() {
        return ref -> VARSIZE_BEGINS;
    }
}